<UserControl x:Class="DeviationModule.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviationModule.Views" xmlns:viewmodels="clr-namespace:DeviationModule.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:EditorViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Coral">
        <TextBlock Margin="10"  Text="{Binding SelectedProcedure.Name}"></TextBlock>
    </Grid>
</UserControl>
